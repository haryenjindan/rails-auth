<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h1>Home</h1>
<p>Some content here.</p>

<% if @user.new? %>
  <p>
    Sign up in seconds, or <%= link_to "log in", login_path %>.
  </p>
  <%= render 'form' %>
<% else %>
  <p>
    <%= link_to "Log Out", logout_path, method: :delete %>
  </p>
<% end %>